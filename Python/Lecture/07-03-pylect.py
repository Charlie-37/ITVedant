# Local Variable : the variable inside a particula function is only acessiable inside that function.
# Local scope: It is only accessiable inside its own scope.

# Global Scope: It can be both accessable inside or outside a function
# Global Variable: variable declared in a global scope is both accessable inside or outside a function.7




